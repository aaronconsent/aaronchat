# OVERNIGHT BUILD BRIEF — GovCon / SAM.gov outreach landing infrastructure

You are executing a scheduled overnight build for Aaron Phillips (Hey Aaron! Marketing).
This brief is self-contained. Read it fully, then build → deploy → verify. Work autonomously
and finish so it's ready when Aaron wakes up. Also read the source spec:
`/Users/aaronphillips/Downloads/sam-gov-outreach-system.md` (the full outreach system).

## Scope (what to build — confirmed with Aaron)
1. `aaron.chat/govcon/` landing page (deploy).
2. `aaron.chat/govcon-checklist/` lead-magnet page + email capture (deploy).
3. `.docs/govcon-email-templates.md` — the 4-touch NEW-REG + RENEWAL sequences as ready-to-send templates.
4. `.docs/govcon-runbook.md` — one consolidated, execute-in-order setup + operating runbook.
Do NOT build the SAM→D1 data pipeline, DNS, Gmail, enrichment, or booking tool — those need Aaron's
logins. Document them in the runbook as a "Blocked on you" checklist instead.

## Repo & deploy conventions (follow exactly)
- Repo / git root: `/Users/aaronphillips/GIT/aaron-chat-mirror/site` (git root IS `site/`).
- Deploy = `git push origin main` → Cloudflare Workers Builds auto-deploys (~5–8 min). No local wrangler.
  Commit style ends with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. If push is rejected,
  `git pull --rebase` then push (scheduled autoposting commits advance the remote).
- Pages are generated by Python scripts in `scripts/` that extract the shared chrome from `index.html`.
  Study `scripts/build_plan.py` for the exact pattern (SPRITE/HEADER/TAIL extraction via string indices,
  `VER = re.search(r"ha\.css\?v=(\d+)", IDX)`, writing `<dir>/index.html`). CREATE `scripts/build_govcon.py`
  that generates BOTH `govcon/index.html` and `govcon-checklist/index.html` the same way.
- Asset version: `ha.css?v=N` is currently around v=151 (read the real current number from index.html).
  Bump it by 1 across ALL html + py: `find . \( -name "*.html" -o -name "*.py" \) -exec sed -i '' 's/?v=OLD/?v=NEW/g' {} +`
  then regenerate the affected pages (build_govcon.py, and any generator that reads VER).
- Design system: `brand/ha.css` (navy #002f62 / #074588 primary, Inter, burnt-orange --tertiary #7a4a12 /
  --tertiary-bright #b4610c, tokens --r/--r-lg, --sh-sm/md). Reuse existing classes: `.page-hero`, `.sec`,
  `.sec-low`/`.sec-white`, `.wrap`, `.prose`, `.btn .btn-call/.btn-primary/.btn-lg`, `.card`, `.faq`, the
  sprite icons (`#i-phone`, `#i-check`, `#i-shield`, `#i-search`, `#i-target`). Don't invent a new look;
  match the rest of the site. Add only minimal new CSS to `brand/ha.css` if truly needed.
- Verify after deploy: poll `https://aaron.chat/govcon/` and `/govcon-checklist/` until HTTP 200 and the
  new copy is present (curl loop, ~20s intervals). Confirm the capture form posts to `/api/lead`.

## Voice & integrity (NON-NEGOTIABLE)
- Voice guide is LOCKED: `brand/voice-guide.md` (+ on-site /style-guide/voice/). Read it first. First-person
  Aaron, plain East-Texas owner-operator, StoryBrand + Ryan-Reynolds informality, napkin-math / oddly-
  specific-stat / anti-pitch signature moves, banned-lexicon list. The GovCon hero angle = "you're the guy
  WARNING them about the renewal scams," not another pitch.
- INTEGRITY: NO fabricated testimonials, client counts, metrics, certifications, or partner badges. NO
  invented government affiliation. The ONLY proof allowed here is Aaron's own real, verifiable SAM
  registration (UEI **JN2VF5LTSE83**, verifiable at sam.gov) and that he's a real agency in Coldspring, TX.
- Never state or imply Hey Aaron! is SAM.gov, GSA, or government-affiliated.

## Real business facts (use these)
- Aaron Phillips · Hey Aaron! Marketing · aaron.chat · hello@aaron.chat · 713-384-8985
- Physical address (CAN-SPAM + trust, put in page footers/signatures): 50 Harbour Lane, Coldspring, TX 77331
- Region ref for copy: East Texas / Coldspring / Livingston area
- SAM UEI: JN2VF5LTSE83
- No Calendly/Cal.com booking URL exists yet → CTAs use the phone (713-384-8985) + the existing `/quote/`
  form. Note in the runbook that a booking link can be swapped in later.
- Email capture: reuse `/api/lead` (handled by `handleLead` in `_worker.js`, sends via Resend to Aaron).
  Inspect `handleLead` + an existing form (e.g. the quiz/callback on index or `/quote/`) for the exact
  field names it expects, and match them so the form actually delivers.

## Page 1 — /govcon/ (indexable, add to sitemap, NOT in main nav)
Purpose: "Marketing for Texas contractors entering government work." Doubles as SEO + makes the cold-email
claims verifiable. Sections (adapt to voice, keep tight):
- **Hero (the pattern interrupt):** lead with the scam warning. e.g. H1 along the lines of "Just registered
  in SAM.gov? First: don't pay anyone to 'renew' it." Subhead: SAM is free, forever, at sam.gov — the
  official-looking $500 renewal emails are all scams. Then: I help East-Texas contractors actually win work
  (government and local). CTA: Call Aaron 713-384-8985 / Get a quote.
- **The scam, explained:** the wave of lookalike-domain renewal invoices; what's real (sam.gov, free) vs. not.
- **What buyers see when they Google a new registrant:** contracting officers & primes look you up first —
  website, Google profile, capability statement. Thin = the phone doesn't ring (federal OR local homeowners/GCs).
- **What I do about it:** GovCon-ready website + a flat-fee one-page capability statement + ongoing marketing.
  Keep it TAYA/transparent, plain pricing language consistent with `/pricing/` (Website & Growth $500/mo,
  Social $500/mo, ads 15% of spend, $2k/mo min). Capability-statement + one-page-site package = flat fee,
  "quoted after a 20-min fit call" (don't invent a specific number if not on /pricing/).
- **Why trust me:** I'm a real agency in Coldspring, TX and I registered my OWN agency in SAM (UEI
  JN2VF5LTSE83 — look it up). I'm the one warning you about the scammers, not one of them.
- **CTA band:** "GovCon Marketing Fit Call — 20 minutes" → phone + /quote.
- Footer already comes from the shared chrome (has address). Ensure a physical address is visible.
- SEO: title/description targeting e.g. "SAM.gov contractor marketing Texas", "capability statement Texas
  contractor", "new SAM registrant website". Self-referencing canonical. LocalBusiness/Service JSON-LD is fine
  (reuse the pattern from other pages) but NO fake reviews/ratings in schema.

## Page 2 — /govcon-checklist/ (indexable, sitemap)
Purpose: the lead magnet referenced in email touch 2. Value-first (low friction — this audience distrusts
gates), so put the actual checklist ON the page:
"**The New SAM Registrant's First 90 Days** — the 5 things to have in place":
1. A real website buyers can find (they Google you before they call).
2. A claimed, filled-out Google Business Profile + first reviews.
3. A one-page capability statement (your NAICS codes, past work, differentiators).
4. A clean SAM record (NAICS, POC, entity URL actually filled in).
5. Never pay to "renew" — it's free at sam.gov; the invoices are scams.
Expand each to 1–2 plain sentences. Then a soft capture: "Want me to look at yours, or get the printable
version? Drop your details and I'll send it + a 20-min fit call" → `/api/lead` form (name, email, company,
optional message). Cross-link to /govcon. Physical address visible.

## Deliverable 3 — .docs/govcon-email-templates.md
Copy the NEW-REG (touches 1–4, incl. the Segment-C no-website touch-2 variant) and RENEWAL (touch 1 + reuse
notes) sequences from the source spec VERBATIM in structure, but:
- List the merge fields used ({{first_name}}, {{company}}, {{trade}}, {{city}}, {{expiration_month}},
  {{your_region_ref}} → "East Texas").
- Fill the signature block on every touch with the real NAP:
  `Aaron Phillips · Hey Aaron! Marketing · aaron.chat` / `50 Harbour Lane, Coldspring, TX 77331 · 713-384-8985`.
- Keep the opt-out line on every email. Keep subject lines. Format as copy-paste-ready blocks.

## Deliverable 4 — .docs/govcon-runbook.md
One execute-in-order runbook consolidating the source spec's operational sections, with real values filled in:
- **Phase 0 — Infra (BLOCKED ON AARON, but give exact values):** DNS SPF `v=spf1 include:_spf.google.com ~all`;
  DKIM (enable in Google Workspace Admin → Gmail → Authenticate email, 2048-bit); DMARC
  `v=DMARC1; p=none; rua=mailto:dmarc@aaron.chat` (→ p=quarantine after 30 clean days). Gmail sending account
  (aaron@ or hey@aaron.chat), profile photo, signature (NAP above). Google Postmaster Tools on day 1.
  The two live pages (built tonight) as the landing infra.
- **Phase 1 — List ops:** segments A/B/C, per-record fields required, suppression list rules, enrichment +
  email verification (only send to "valid").
- **Phase 2 — Sending discipline:** the volume ramp table (warm-up 5–10/day → cruise 30–40 max), spread
  8am–4pm CT, one link per email, no open-tracking pixels, bounce >3% = stop.
- **Phase 3 — Sequences:** point to govcon-email-templates.md.
- **Phase 4 — Reply playbook & CRM handoff** (from the spec's table). Metrics/targets, kill switches,
  compliance card.
- **Blocked on you:** a crisp checklist of everything needing Aaron's accounts (DNS, Gmail, SAM.gov API + D1
  pipeline, enrichment vendor, verification vendor, booking tool).

## Finish criteria
- build_govcon.py created; both pages generated, in the sitemap, version bumped, committed, pushed, and
  VERIFIED live (HTTP 200 + new copy present) with the capture form pointing at /api/lead.
- The two .docs deliverables written.
- Do NOT modify existing pages beyond the version bump + sitemap addition. Keep /govcon out of the main nav
  (a footer link is optional/fine). Leave a short summary of what shipped + the "Blocked on you" list where
  Aaron will see it in the morning.
